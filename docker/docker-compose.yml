version: "0.0.1"

services:
  app:
    image: app:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: python ./src/analyze.py
    volumes:
      - ../:/usr/src/app/
    ports:
    environment:
      - TESTING=0
      - ENVIRONMENT=dev

      - DEBUGGER=False